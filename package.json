{
  "name": "@singleton-sd/inkads-epaper-renderer",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript e-paper image renderer for InkAds",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "echo \"Bootstrap pending — see GitHub Issues\" && exit 1",
    "test": "echo \"Bootstrap pending — see GitHub Issues\" && exit 1"
  }
}
